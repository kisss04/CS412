import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    padding: 20,
  },

  scrollContainer: {
    alignItems: 'center',
    padding: 20,
  },

  titleText: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 10,
  },

  bodyText: {
    fontSize: 16,
    textAlign: 'center',
    marginBottom: 10,
  },

  image: {
    width: 200,
    height: 200,
    marginTop: 10,
  },
});